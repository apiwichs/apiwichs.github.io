import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://apiwichs.github.io',
  // If you deploy to a REPO (not username.github.io), uncomment:
  // base: '/your-repo-name',
});
